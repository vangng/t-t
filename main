CREATE DATABASE shop_acc;
USE shop_acc;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50),
    password VARCHAR(100),
    balance BIGINT DEFAULT 0
);

CREATE TABLE products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    price BIGINT
);

CREATE TABLE accounts (
    id INT AUTO_INCREMENT PRIMARY KEY,
    product_id INT,
    account TEXT,
    sold TINYINT DEFAULT 0
);

CREATE TABLE transactions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    order_id VARCHAR(100),
    amount BIGINT,
    status VARCHAR(20)
);

INSERT INTO users (username,password,balance) VALUES ('vang','123456',0);

INSERT INTO products (name,price) VALUES 
('VIA Cổ',20000),
('VIA Việt',10000);

INSERT INTO accounts (product_id,account) VALUES 
(1,'via1|pass1'),
(1,'via2|pass2'),
(2,'via3|pass3'),
(2,'via4|pass4');
